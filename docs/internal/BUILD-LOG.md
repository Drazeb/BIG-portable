# Changelog — Brand Identity Generator (BIG)

## 2026-05-28
- **Suppression de la Phase 2C (Concept de Réconciliation) — pipeline 13→12 étapes (D60)** — Suppression complète de l'étape "Concept de Réconciliation" (subagent `phase-2c-reconciliation.md` supprimé, section `## CONCEPT DE RÉCONCILIATION` retirée du filtrage `scoping-filtered.md`, variable `{reconciliation_concept}` éliminée). Renumérotation cascade : ancienne 2D (Territoires Créatifs) → 2C, ancienne 2E (Mix pondéré) → 2D, ancienne 2F (Choix du mode) → 2E. Pipeline user-facing passe de 13 à 12 étapes. Tension de Marque + Curseurs A×B **conservés** (toujours opérationnels — Curseur B module les Ventres Mous Phase 3B depuis D45, les 2 pôles alimentent la section "Tension résolue" Phase 3A). **Pourquoi** : la brique était un placebo fonctionnel documenté depuis D34/D35 (mars 2026) qui avaient déjà retiré `{reconciliation_concept}` du prompt Phase 3A avec zéro régression mesurée — *"le territory mix le supersède"*. 9 tentatives historiques (D25-D33) pour en faire un moteur créatif avaient échoué (plafond LLM ~2-4/10 métaphores pertinentes). Friction UX maximale pour utilisateurs externes ("choisis le trait unique qui englobe tes 2 pôles" = étape la plus opaque du pipeline) à un moment où BIG s'externalise. **Audit triple effectué** (3 agents + mini-audit 4 zones d'ombre) confirme zéro dépendance cachée : aucun script Python, aucun skill aval (brand-book, landing-page, visual-brief, visual-prompt), mode aspiration (1D) saute toute la Phase 2. Une seule dépendance technique : 1 ligne dans la liste de filtrage de `scoping-filtered.md` (SKILL.md). **Gain net** : 1 tour user en moins (le plus difficile à expliquer), 1 subagent en moins (latence + coût). À tester E2E via `/test-big` sur session Camille ou VoltaPilot — démarrer à la nouvelle Phase 2C (Territoires Créatifs), vérifier extraction cohérente + non-régression downstream Phase 4. Fichiers : `phases/phase-2c-reconciliation.md` (SUPPRIMÉ), `SKILL.md` BIG (bloc Étape 2C retiré + scoping-filtered allégé + renumérotation 2D→2C/2E→2D/2F→2E + message transition), `phases/phase-3a-evaluate.md` (ligne `Concept de reconciliation` retirée du format trace), `ref/pipeline-overview.md` + `ref/pipeline-overview-public.md` (section "3bis. Réconciliation" supprimée), `test-big/SKILL.md` (5 zones : liste user, prérequis, outputs, algo `phases_ordonnées`, mapping), `CLAUDE.md` projet (pipeline 13→12), `ARCHITECTURE.md` (chapeau Phase 2 + Phase 2 sous-le-capot 5→4 étapes + Phase 3A "Ancrage = territory mix"), `DECISIONS.md` (D60), MEMORY. Plan canonique : `~/.claude/plans/c-et-bon-streamed-pixel.md`.

## 2026-05-27
- **Router famille d'icônes (Phase 6A-0) + refonte chapitre 06 du Batch 2 en 3 sections orientées USAGE (D59)** — Refonte structurelle de la Phase 6A : nouvelle étape fille 6A-0 (sous-agent isolé qui choisit UNE famille parmi 8 — pictogramme géo / isométrique / pixel / gravure / ornemental / flat illustré / sticker / brutaliste — sur la base de la fiche styliste + concept + brief + ventre mou, scan binaire COMPATIBLE/INCOMPATIBLE avec pool randomisé Fisher-Yates anti-autoroute, output `{brand}-icon-family-choice.md` avec marqueurs `CHOIX_FINAL:` + `BACKUP:` parsables, validation user obligatoire) puis étape 6A-1 (subagent Batch 2 actuel, refondu pour produire chapitre 06 en 3 sections : **06.1 Set d'icônes UI** 18-22 icônes utilisables — 10-12 UI primaire + 4-6 UI métier + 4 statuts ; **06.2 Traitements alternatifs** 1-2 max étiquetés cas d'usage business explicite ; **06.3 Usage en contexte** 1 mini-mockup avec squelette imposé restreint à sidebar/table+toolbar/toolbar/breadcrumb/list/nav, pas de duplication chapitre 04). Audit anti-slop fin de batch enrichi de 2 points (icônes UI pas illustrations narratives + squelette mockup respecté). **Pourquoi** : 10 batches BIG audités sur 3 marques (Camille/VoltaPilot/Vermeil) → zéro variation de famille graphique car "Outline/Solid/Duotone" gravé 3× en dur (`phase-6a-batch2.md:129/163`, `master-style-guide.md:108`) est nomenclature native d'UNE seule famille (pictogramme géo). Les 4 sous-sections didactiques précédentes (Grammaire/Variations/Abstraction/DA Illustrative) poussaient à des illustrations narratives (sextant, phare, scène d'auscultation) inutilisables en production. **PoC empirique** dans `tools/icon-system/` validée sur 3 sprints Camille + VoltaPilot (router choisit familles DIFFÉRENTES selon concepts : Camille→gravure / VoltaPilot→flat illustré cinéma ; designer produit set utilisable). **Migration** : `tools/icon-system/{catalogue,slop-sheets,references,prompts}/` → `.claude/skills/brand-identity/ref/icon-system/` (48 fichiers — 8 catalogue + 8 slop sheets + 24 références PNG + 3 prompts + index + SOURCES). Script `randomize_pool.py` copié dans `.claude/skills/brand-identity/scripts/`. PoC archivée dans `tools/icon-system/_archive-poc/` après validation. **Pas de feature flag — rewrite franc**. À tester E2E via `/test-big` redémarrant à `6A` sur session Camille 0513 (famille attendue : gravure ; backup : ornemental) puis test croisé VoltaPilot 0504 (famille attendue : flat illustré cinéma). Fichiers : `phases/phase-6a-0-icon-router.md` (nouveau), `phases/phase-6a-batch2.md` (refonte L127-132 + L163-166 + audit + variables), `ref/master-style-guide.md` (L107-111 refonte section 06), `SKILL.md` BIG (bloc Étape 6A-0 inséré avant L5552 + variables passées à 6A-1), `ref/icon-system/` (nouveau dossier 48 fichiers), `scripts/randomize_pool.py` (copié et adapté `--output`), `test-big/SKILL.md` (3 tables : prérequis 6A + outputs 6A + mapping phase), `DECISIONS.md` (D59), MEMORY.
- **Refonte mécanique aversions client — suppression Phase 1bis + 3B-0b + diagnostic compat Phase 2A ; ajout collecte aversions + 2 mini-checks post-génération (D58)** — Suppression complète de la brique "préférences esthétiques / inspirations" introduite par D22 (~300 lignes orchestrateur + Phase 1bis subagent multimodal + 6 placeholders dans les prompts subagents Phase 3B + 2 fichiers session `aesthetic-profile.md` / `aesthetic-selections.md`). Remplacement par : (1) Collecte minimale d'aversions dans l'interview Point 15 (couleurs en description libre + registres en Q/R adaptatif max 2 relances avec tag FLOU si non cadrable). (2) Stockage dans section "## Aversions client" en fin de `brief-analysis.md`. (3) Conservation simplifiée du diagnostic température Phase 2A (dérive depuis brief + aversions, plus depuis inspirations), stocké dans nouveau mini-fichier `{brand}-validated-temperature.md` consommé par le routeur 3B-0. (4) 2 mini-checks LLM advisory non-bloquants greffés sur les 2 checkpoints user déjà existants : Vague 1-choix palette (avant affichage planche, 1 Task par concept compare hex aux aversions couleur → alertes ⚠ ajoutées au message) + 3B-7-checkpoint (avant tableau choix style, 1 Task par concept compare style retenu + signatures aux aversions registre → alertes). **0 nouveau gate Python, 0 nouvelle pause user, 0 injection amont** (la génération reste 100% aveugle aux aversions). **Pourquoi** : REX 17 tests mars-avril 2026 avait démontré que retirer ou ajouter le profil esthétique ne changeait PAS la convergence chromatique. Charles ne l'utilisait quasi jamais sur ses propres runs. Encapsulation BIG en app externe → opportunité de nettoyer. Audit dépendances exhaustif effectué avant (14 angles morts vérifiés, 0 résiduel) : 0 dépendance bloquante, 0 dépendance scripts Python ni skills externes (brand-book, landing-page). Implémentation en 7 lots dans l'ordre cardinal "consommateurs avant producteurs". Renommage test-big : `3B-1a` → `3B-1`, suppression `3B-1b` (puisque 3B-0b côté BIG est supprimée). Tests E2E à faire : pipeline sans aversion + pipeline avec aversions claires + reprise session héritée. Plan canonique : `~/.claude/plans/ok-coute-tu-peux-typed-biscuit.md`. Fichiers : `phase-1bis-aesthetic.md` (supprimé), `SKILL.md` BIG (~280 lignes retirées + ~70 ajoutées), `phase-2a-scoping.md` (réécrit simplifié), 6 prompts subagents Phase 3B, `big-brief-interview.md` + v2, `phase-1-brief.md`, `test-big/SKILL.md` (13 lignes + 5 endroits structurels), `visual-brief/SKILL.md:112`, `ref/pipeline-overview.md`, `ref/styles-matching-protocol.md:21`, `outputs/phase-3b-pipeline-map.md` (supprimé), `DECISIONS.md` (D58), MEMORY.

## 2026-05-14
- **Mode Sélectif par registre — alternative au mode Génératif en Phase 3A (D57)** — Ajout d'un 3e mode à l'Étape 2F (à côté de Génératif libre + Génératif orienté registre) : le mode **Sélectif par registre** demande à l'utilisateur un registre dans `ref/registres-creatifs.md` (catalogue 28 registres existant) puis enchaîne 10 sous-étapes : 5 sub-agents vierges parallèles → pool ~210 mots → Python dédup + sélection 100 → Python split 10 batchs random → 10 sub-agents évaluateurs parallèles (scan obligatoire EXPLOITABLE/INEXPLOITABLE, choix unique, dynamique narrative, justification chirurgicale par comparaison, auto-test brief-first) → 1 sub-agent explicateur neutre (définitions factuelles anti-biais) → récap MarkView → user choisit max 3 → assemblage au format compatible Phase 3B. Mode-agnostique pour la 3B aval (même fichier de sortie `{brand}-concepts-narratifs-v{N}.md`). L'accumulation cross-batch et la sélection finale libre existantes sont réutilisées telles quelles → on peut alterner v1 Sélectif registre A + v2 Sélectif registre B + v3 Génératif libre dans la même session. **Pourquoi** : le mode Génératif produisait parfois des noms artificiellement complexifiés type "Le Phare de Ralliement" — le mode Sélectif **rend structurellement impossible** la complexification (le LLM ne génère plus un nom, il choisit dans un pool de 100 mots tirés du registre). Test empirique sur Camille × signalisation maritime : "phare" EXPLICITEMENT écarté comme cliché par le sub-agent du batch correspondant au profit de "sémaphore" — la mécanique anti-cliché fonctionne. **Patterns réutilisés du système polices** : scan obligatoire avec verdict binaire (anti-bâclage), justification chirurgicale par comparaison (anti-rationalisation), pas de scoring numérique, parallélisme strict (évite round-robin). **Règle "1 mot" assouplie** pour accepter composés courts limpides ("chenal balisé", "lentille de Fresnel", "haut-fond"). **Migration globale TextEdit → MarkView** pour l'ouverture du catalogue de registres + le récap des concepts. Le mode Génératif existant reste intact et disponible — Charles choisit au cas par cas. test-big : pas de nouveau code, `3A1` couvre les 3 modes (le mode est choisi à l'Étape 2F). Mini-app de référence `tools/registre-explorer/` (labo R&D) reste en place. À tester E2E via `/test-big` redémarrant à `3A1`. Plan canonique : `~/.claude/plans/c-es-bon-cryptic-river.md`. Fichiers : `phases/phase-3a-selectif-pool.md` + `phase-3a-selectif-evaluator.md` + `phase-3a-selectif-definitions.md` + `phase-3a-selectif-batch-assemble.md` (nouveaux), `scripts/phase3a-selectif-build-pool.py` + `phase3a-selectif-split-batches.py` (nouveaux), `SKILL.md` BIG (Étape 2F + nouvelle Étape 3A Mode Sélectif + Checkpoint élargi à 5 options + MarkView), `ref/registres-creatifs.md` (footer MarkView), `test-big/SKILL.md` (3 zones), `DECISIONS.md` (D57), `ARCHITECTURE.md`, MEMORY.
- **Amendement D54 — retrait de la cover band Batch 2** (entrée éditoriale image ouvrant le Batch 2, ajoutée par D54 le 12 mai) — jugée « moche » à l'usage (Charles, après visualisation du livrable Camille `test-camille-test-20260513-1453`). Le Batch 2 ouvre désormais comme le Batch 3 : `<a class="skip-link">` → `<main id="main">` → **kicker éditorial sobre** (« Volume II · Système de Signes », overline 1px gauche, color `var(--color-beacon)`/`var(--color-accent)`) → chapitre 05 normal. Plus aucune référence à `visual-final/` côté Batch 2 — le batch redevient strictement self-contained. La mention du volume vit dans le `<title>`, le kicker, et le footer existant. **La librairie `visual-final/` reste consommée par Batch 3 ch.08/ch.10** — c'était la valeur principale de D54 (les images dérivées remplacent un faux moodboard CSS par les vraies images). Fichiers modifiés : `phases/phase-6a-batch2.md` (section « BANDE DE COUVERTURE (conditionnelle) » → « ENTRÉE ÉDITORIALE (obligatoire, sobre) »), `SKILL.md` (étape Inventaire — retrait du routage cover band + variable `{cover_visual_rel}` non injectée + références maj), `ref/html-showroom-spec.md` §8 (Batch 2 retiré de la consommation `visual-final/`), `ref/visual-final-batch-integration-rex.md` (sous-section « Amendement 2026-05-14 »), `DECISIONS.md` D54 (amendement), `CHANGELOG.md`, MEMORY. Maj manuelle de `outputs/test-camille-test-20260513-1453/camille-batch2-le-phare-de-ralliement.html` (suppression CSS `.batch2-cover*` + HTML section + ajout `<main>` + kicker `.batch2-volume-marker`) ; re-run du gate Batch 2 → PASS, 8 warnings informationnels (inchangé).

## 2026-05-13
- **Corrections post-audit du gate anti-slop Batch 2/3 + couverture régex Phase 4 étendue (suite de D56)** — Trois corrections suite à l'audit du jour, déclenché par un faux PASS sur Camille 0513 batch2 où Charles a repéré visuellement un accent-bar via `box-shadow inset 2px 0 0 var(--color-ocre)` au hover d'une `.icon-card`. Investigation : (1) **A1 trou de couverture confirmé empiriquement** — `box-shadow: inset Npx 0 0 color` (contournement explicitement cité par Impeccable BAN 1, L213 : « Do not just swap to box-shadow inset ») n'était couvert par **aucun** check. 5/6 canoniques récents post-P17 (Camille 0511/0512/0513, VoltaPilot 0429) shippent avec ce pattern. Fix : 3ᵉ pattern régex ajouté à `check_accent_bar` dans `scripts/phase4-blacklist-gate.py`, scan multi-ligne (`re.DOTALL`) car la valeur box-shadow s'étale souvent sur plusieurs lignes. Bénéficie à la Phase 4 ET au wrapper batch (qui délègue). (2) **A2 nouveau check `check_outline_focus_visible_pairing`** — toute règle `outline: none/0` doit être compensée par un `:focus-visible` qui restaure un outline visible (Vercel Focus States + GStack item 5). Empirique : 0/6 canoniques récents fuient effectivement (tous ont un `:focus-visible` global qui restaure), mais le check est un filet de sécurité futur sans bruit ajouté sur l'historique. (3) **A3 inversion du défaut erroné dans `phase6-batch-gate.py`** — la calibration d'hier mettait tout en WARN par défaut et promouvait une petite liste à FAIL (« période d'observation »). Conséquence : sur Camille 0513 batch2, le `.alert` L1401 (`border-inline-start: 3px solid var(--color-ok)`) détecté par le gate mais classé WARN → ship du slop sans alerte. Correction : **garder la sévérité native du gate Phase 4 par défaut**, supprimer `BATCH_FAIL_ALLOWLIST`, introduire `BATCH_DEMOTE_TO_WARN` court et justifié (`R148` theme-color, `R054` skip-link, `hover_multi_property`, `advanced_techniques` — ce dernier ajouté car le finishing gate exige 4 techniques avancées calibrées style-tile, alors que les prompts batch n'en mandatent que 2). **Validation E2E** sur Camille 0513 batch2 : verdict passe de PASS (faux) à FAIL avec 3 violations `other_fails` surfacées (2× accent-bar dont l'inset L526 + box_shadow_layers). **Corpus offline** : 88 batches scannés, 0 PASS (les batches historiques contiennent vraiment du slop, c'est le comportement attendu) ; top surface = accent-bar 55×, external-image 44×, hover-translateY 35×. **L'audit catégorie A** (18 trous théoriques cross-référencés au canon audit-slop) a montré que 15 sur 18 ne fuitent pas empiriquement (bloqués structurellement par les gates Phase 3B fonts/palette ou par les prompts Phase 4). Le faux « 30 % de fuite border-left ≥2px » de l'audit initial était un biais d'historique (97 % pré-mai 2026, donc pré-toutes-les-optimisations actuelles) — sur sessions récentes post-P17 (suppression du rollback abusif), 0 / 6 de fuite en pipeline complet. Et la « cause B rollback » diagnostiquée à tort sur une session du 27 avril (`P3 ROLLBACK`) était basée sur une logique supprimée le 1ᵉʳ mai par commit `6c2780d` (P17). **C — Clarification test-big mode partiel** : note explicite dans `test-big/SKILL.md` + bannière dans `.test-context.md` (« ⚠️ Mode test — pipeline anti-slop partiellement actif ») quand `start_phase ≠ 1`, car 3 sessions empiriques (VoltaPilot 0504, Terra Lente 0502, Karma 0502) shippent avec accent-bar simplement parce que la boucle 4A n'a pas tourné — pas un bug du gate. Fichiers : `scripts/phase4-blacklist-gate.py` (A1 + A2 + ajout à ALL_CHECKS), `scripts/phase6-batch-gate.py` (A3), `test-big/SKILL.md` (C), `CHANGELOG.md`, `DECISIONS.md` (D56 enrichi), `ref/plan-integration-anti-slop.md` (encadré État mis à jour), MEMORY.

## 2026-05-12
- **Gate anti-slop Batch 2/3 + auto-audit de fin de batch (D56)** — Comblement de l'Étape 5 (volet Batch 2/3) du plan `ref/plan-integration-anti-slop.md` (prescrite le 24 avril 2026, jamais implémentée). Deux livrables : (1) **`scripts/phase6-batch-gate.py`** — wrapper mince (~250 lignes) qui tourne **une fois par batch** sur le fichier complet assemblé (Batch 2 après injection des SVG logos ; Batch 3 après assemblage des 3 chapitres), strippe les `<svg>`/`data:` URIs, **délègue** aux gates Phase 4 existants (`phase4-blacklist-gate.py` + `phase4-finishing-gate.py`, ~70 checks), re-pondère leurs verdicts selon un profil « documentation batch », et ajoute des checks batch-spécifiques (**Specs Lock** = diff du `:root` vs celui du style-tile retenu ; **Completeness** = présence des sous-sections obligatoires ; **external-image** ; **Lorem ipsum**). Sortie JSON `{status, deterministic_fails, other_fails, warns}`. (2) Une **section « Audit anti-slop de fin de batch »** (~8 items) dans `phase-6a-batch2.md` et `phase-6b-batch3.md` (auto-audit de jugement du subagent : squint test, cards earn existence, cookie-cutter rhythm, data-ink, Show > Tell). **Câblage SKILL.md** : étape « Gate anti-slop Batch 2 » après l'injection SVG ; « Étape 6B-6 » après l'assemblage Batch 3 ; **advisory par défaut** mais **auto-correction chirurgicale (1 tour max)** sur les `deterministic_fails` uniquement (Specs Lock recollé à l'identique ; sous-section manquante rajoutée ; police bannie remplacée — fixes mécaniques) ; les autres FAIL et les WARN sont **surfacés à l'utilisateur** (« je corrige ? »), pas d'auto-trigger. **Pas** de réplication de la machinerie 4-Critiques/Synthétiseur/boucle 4A de la Phase 4 — choix assumé, conforme à l'arbitrage du 24 avril (les batches sont des planches de documentation, pas le style-tile « héros » ; surcharger leurs subagents dégraderait le comportement). Carte WARN/FAIL « période d'observation » : par défaut tout est WARN (non-bloquant), seuls quelques rule_ids sont promus FAIL (anti-patterns datés visibles : `hover-translateY`, `animation-infinite`, `glow-shadow`, `text-shadow-glow`, `staggered-fadeup`, `hover-letter-spacing`, `underline-scaleX`, `no_generic_ease`, `R-108-emojis`) ; exclusions (faux positifs garantis sur un batch) : `graphic-layer-*`, `figurative-svg`, `stroke-circle-amateur`, `overline-decorative-line`, `root_vars_unused`. Ajout de `--json-output` à `phase4-blacklist-gate.py` (compat ascendante : stdout inchangé — la boucle 4A de Phase 4 le parse encore). Calibration corpus offline : ~85 batch HTML de `outputs/` scannés, **aucun faux positif déterministe** (`completeness`/`R013` ne se déclenchent que sur d'anciens batches réellement incomplets — pré-§04 — ou avec une vraie police bannie ; `specs-lock` passe quand le batch est confronté au `:root` du style-tile retenu). À tester E2E via `/test-big` redémarrant à `6A`. Fichiers : `scripts/phase6-batch-gate.py` (nouveau), `scripts/phase4-blacklist-gate.py` (+`--json-output`), `phases/phase-6a-batch2.md`, `phases/phase-6b-batch3.md`, `SKILL.md` BIG (PHASE 6A + Étape 6B-6 + note agentId Batch 2), `DECISIONS.md` (D56), `ARCHITECTURE.md`, `ref/plan-integration-anti-slop.md`, `test-big/SKILL.md` (note), MEMORY.
- **Nouvelle Étape 5D — couche d'animation optionnelle du style-tile (D55)** — Dernière étape du bloc style-tile (après le choix final Phase 5, avant la Phase Logo / Batch 2). Optionnelle (proposition Oui/Non). Tourne sur le style-tile retenu uniquement. Pattern Phase Logo : 5D-0 proposition → 5D-1 analyse + preset recommandé (orchestrateur inline : lit le style-tile, détecte un overlay calé sur l'image → `hero_safe_mode`, lit le registre, choisit un preset dans `ref/animation-catalogue.md` § « Presets recommandés », génère `{brand}-animation-menu.md` ouvert en MarkView) → 5D-2 ajustement/validation user → 5D-3 sous-agent animateur (`phases/phase-5d-animation.md`, produit 2-3 variantes de dosage subtil/médian/prononcé) → 5D-4 ouverture navigateur + itération (resume agentId) → 5D-5 promotion variante retenue en `{brand}-style-tile-concept-{n}-animated.html` + archive `_archive-anim-{round}/` + `{brand}-animation-spec.md`. Le packaging final copie aussi `{brand}-style-tile-animated.html` + `{brand}-animation-spec.md` si `{animation_done}` = true. **Stack** : GSAP 3.13 (ScrollTrigger + SplitText) via CDN + CSS natif ; **pas** de smooth-scroll par défaut (scroll natif — le smooth-scroll donne une sensation « lourde », il reste opt-in) ; **pas** de WebGL/3D/vidéo. **Non-régression stricte** : couche purement additive (`<script src>` CDN + un `<style>` scopé `html.gsap-anim …` + des `data-` neutres + un `<script>` d'init en IIFE avec garde-fou `if (reduceMotion || !window.gsap …) { revealAll(); return; }`) ; le `:root`/`@layer tokens`/CSS validé/`<script>` existants ne sont jamais touchés ; le style-tile statique reste intact et coexiste avec la version animée. **Mode sûr hero** : si le hero a un overlay calé sur le cadrage de l'image (SVG/canvas géométrie pixel — ex : faisceau de phare), pas de zoom/pin/parallaxe multi-couches sur le hero, on n'anime que le texte. **Catalogue à 6 axes** (`ref/animation-catalogue.md`) : A scroll (natif/lissé), B entrée hero (aucune/fade-up/split-text lignes-mots-lettres/clip-path/blur-in/compteurs), C hero au scroll (parallaxe 1 couche/multi-couches/fade-out/pin&scrub/Ken Burns/header condensé — colonne compat overlay), D reveals sections (fade-up/staggered/clip-path wipe/scale-in/slide-in/line drawing/compteurs/image clip/parallaxe intra-page), E micro-interactions (hover riches/bouton magnétique/curseur custom/tilt 3D/marquee/hover image), F fond (statique/dégradé animé/grain animé/aurora-mesh ⚠slop/particules ⚠daté/SVG-canvas sur-mesure/vidéo-shader hors v1) + 6 presets (P1 Sobre-Sûr pour mode sûr, P2 Éditorial-Cinétique, P3 Premium-Profond, P4 Tech-Net, P5 Minimal, P0 reveals seuls). Recettes GSAP/CSS par option + garde-fous dans `ref/animation-implementation-guide.md`. **Prototypes de référence** validés sur le style-tile Camille c3 (faisceau de phare) : `outputs/test-camille-test-20260511-1330/camille-style-tile-concept-3-palette-B-creme-ANIM-v3.html` + variantes par effet `-ANIM-1-typo-load` / `-2-parallaxe` / `-3-pin-zoom` / `-v2-phare-safe`. À tester E2E (via `/test-big` redémarrant à `5D`). Reporté v1.1 : gate Python de non-régression (diff du bloc `@layer tokens`), variante GSAP inliné pour livrable autonome. Fichiers : `ref/animation-catalogue.md` (nouveau), `ref/animation-implementation-guide.md` (nouveau), `phases/phase-5d-animation.md` (nouveau), `SKILL.md` BIG (Étape 5D entre Phase 5 et Phase Logo + packaging + transitions + rappels clés), `test-big/SKILL.md` (5 zones : liste user, prérequis, outputs, `phases_ordonnées`, mapping + démarrage 5D + note 6A), `ref/pipeline-overview.md` (étape 7bis), `CLAUDE.md` projet (pipeline + table refs), `DECISIONS.md` (D55), `ARCHITECTURE.md` (brique Étape 5D + diagramme + table refs), MEMORY.
- **Visuels finaux dérivés intégrés dans Batch 2 (cover band) + Batch 3 (ch08/ch10) (D54)** — Quand une session contient une librairie `visual-final/` (7 types : hero, animation, atmosphere ×4 intensités, closeup, macro, pov, schema ; naming `{brand}-c{N}-{paletteID}-{type}[-{variante}].{ext}` ; produite hors pipeline par `/visual-prompt`), l'orchestrateur la détecte (nouvelle étape Phase 6A "Inventaire des visuels finaux dérivés") et l'injecte : Batch 2 ouvre sur une **bande de couverture éditoriale** (1 visuel — hero de préférence) ; Batch 3 **chapitre 08** affiche les visuels réels (atmosphere/macro → 08.1 moodboard + 08.2 color grading où les 4 niveaux d'intensité sont la démonstration littérale ; hero/pov/animation → 08.3 scénographie ; closeup → 08.1) en remplacement des cartes CSS "descriptives" ; Batch 3 **chapitre 10** affiche les schemas en contexte. Embedding par **chemin relatif** (`<img src="visual-final/…">`, `<iframe>` pour les `.html`), jamais en base64 → un Batch 2 avec cover et un Batch 3 avec visuels ne sont plus 100 % self-contained, le dossier `visual-final/` est livré avec (même logique que les vidéos). Multi-palettes → l'orchestrateur demande à l'utilisateur laquelle correspond au style-tile retenu (cas rare). `visual-final/` absent → Batch 2 et Batch 3 strictement inchangés (fallback total). Nuance la règle `html-showroom-spec.md` §8 "pas propagés aux Batches" (reste vraie pour l'image-pivot base64, pas pour la librairie dérivée). Variables : `{cover_visual_rel}` (Batch 2), `{visual_library_ch08}` / `{visual_library_ch10}` (Batch 3). Convention de rangement : cf. passation `outputs/test-camille-test-20260511-1330/PASSATION-batch3-utilisation-visuels.md`. À tester E2E sur la session Camille (visual-final/ complet pour c3). Fichiers : `SKILL.md` BIG, `phases/phase-6a-batch2.md`, `phases/phase-6b-batch3.md`, `ref/html-showroom-spec.md` §8, `test-big/SKILL.md` (prérequis + copie `visual-final/` + exemples + liste user), `DECISIONS.md` (D54), `ARCHITECTURE.md`, `ref/visual-final-batch-integration-rex.md` (nouveau), MEMORY.

## 2026-05-05
- **Routeur chromatique 3B-0a — passage en mode exhaustif (D53)** — Le routeur passe du mode sélectif (génération ad hoc de ~10-15 gammes "discussion-worthy") au mode exhaustif (scan systématique d'un catalogue canonique de ~45 sous-gammes du spectre). Trois changements : (a) nouveau fichier `ref/chromatic-spectrum-catalog.md` qui liste 14 familles de teintes × 2-5 sous-variantes, inliné dans le prompt routeur via `{spectrum_catalog}` ; (b) 3 catégories d'output au lieu de 2 — `Gammes recommandées` (cible 10-15, plafond 18), `Gammes non recommandées` (étrangères ou redondantes fonctionnellement avec une recommandée — réserve d'arbitrage utilisateur au checkpoint), `Gammes fortement non recommandées` (contradictions franches) ; (c) règle anti-inflation : plafond 18 vérifié mécaniquement par le gate `phase3b-gamut-router-anti-slop.py` (au-delà = FAIL et rerun avec feedback). Renommage UI uniquement — le markdown produit garde `Gammes autorisées` / `Gammes exclues` / `Gammes non applicables` pour ne pas casser le gate ni les phases aval. Tests E2E : Camille 16/11/18 = 45, VoltaPilot 18/11/18 = 47, gate PASS au 1er run après 3 itérations de calibration des règles. Continuité parfaite avec l'output sélectif d'origine (9/9 retenues). Nouvelles autorisées 8/9 pertinentes (bordeaux notarial, cuivres patinés, or vieilli, verts forêts — toutes identifiées par l'utilisateur comme angles morts du mode sélectif). Risque résiduel : le subagent palette aval scanne 18 gammes au lieu de 9, à monitorer en condition réelle. Planche HTML modifiée : 3 sections dans l'ordre recommandées → non recommandées → fortement non recommandées, retrait des filtres opacité/grayscale (les couleurs s'affichent en clair pour permettre l'analyse visuelle), distinction par bordure latérale (rouge/grise) et rature des noms d'exclues. Fichiers : `ref/chromatic-spectrum-catalog.md` (nouveau), `phases/phase-3b-gamut-router.md` (refonte), `SKILL.md` (variable `{spectrum_catalog}` + JSON config 3 sections), `lib/gamut-visual.mjs` (3e section + nouvelle terminologie + retrait filtres), `scripts/phase3b-gamut-router-anti-slop.py` (parser 3 sections + check inflation max 18 + check couverture min 30), `DECISIONS.md`, `ARCHITECTURE.md`, MEMORY.

## 2026-05-04
- **Phase 4 artefact hybride — 5 catégories de quotas + ancrage support n°1 du brief + grammaire interne libre (D52)** — La liste rigide de 15 atomes nommés (KPI dominant + segmented control + table + alerte + boutons primary/secondary + input + 2 cards + 5 niveaux typo + badge + navigation hint) est remplacée par une structure hybride à 3 leviers : Étape 1 = respect du support n°1 du brief (le style dit COMMENT signer, pas QUOI produire à la place), Étape 2 = grammaire interne posée par le designer en commentaire HTML (1-3 lignes), Étape 3 = 5 catégories fonctionnelles avec quotas minimum (typographie ≥4 niveaux, donnée ≥2, état ≥1, action ≥1, identité brand ≥1 = ~9 atomes minimum). Anti-contamination stricte : aucun exemple concret d'archétype ou de format d'atome dans les consignes. Test démontré sur 3 cas en condition réelle : Karma → écran-session monolithique avec mantra dominant ; Terra Lente → carnet d'atelier ouvert ; VoltaPilot → dashboard d'impact traité en spread magazine éditorial sombre (l'ancrage support empêche la fuite du dashboard quand le brief le demande). Test Batch 2 (chapitre 04) sur VoltaPilot hybride confirme que la dérivation des composants UI manquants (input, toggle, checkbox, select, badges, alerts) tient en cohérence avec le design language. Règle chromatique annexe : Batch 2 peut introduire AU PLUS 1 token sémantique de feedback supplémentaire si la palette de base ne suffit pas. Test E1 post-intégration validé sur Karma (subagent vierge, sans phrase directrice). Fichiers : `phase-4-artefact.md` (refonte L19+L57-99+gates), `phase-6a-batch2.md` (L115 + règle chromatique), `ref/html-showroom-spec.md` (L186-195), `DECISIONS.md`, `ARCHITECTURE.md`, MEMORY.

## 2026-04-29
- **Inversion ordre 3B : styles AVANT direction visuelle (D51)** — La séquence Phase 3B post-spécimens devient `3B-7a (styliste) → 3B-7b (spécimens stylisés) → 3B-7-checkpoint → 3B-7c (penseur visuel) → 3B-7d (pitch) → 3B-7e (génération visuels MJ/Recraft)`. Le penseur visuel reçoit désormais la fiche styliste retenue (`{style_choice}` injectée post-checkpoint) et DÉRIVE son ancre stylistique des signatures et références culturelles de la fiche, au lieu d'inventer librement. Renommages : 3B-5 (penseur) → 3B-7c, 3B-7c (pitch) → 3B-7d, 3B-6 (génération visuels) → 3B-7e. Mécanisme central : (a) bloc "STYLE OFFICIEL FAIT AUTORITÉ" en intro du prompt penseur visuel, (b) recâblage Étape 4bis — chaque dimension de l'ancre cite la phrase de la fiche qui la justifie, (c) DNA visuel transmissible (3-5 lignes) en fin d'output pour calibrer le générateur de prompts MJ/Recraft, (d) critère "Fidélité au style retenu" ajouté à la gate qualité. Test E2E à venir. Commit : `6771bec`. Fichiers : `SKILL.md` BIG (déplacement bloc Vague 4 + 9 cross-refs patchées), `phase-3b-penseur-visuel.md` (~35 lignes ajoutées), `phase-3b-design.md`, `test-big/SKILL.md` (5 zones patchées).
- **Inversion typo ↔ palette en Phase 3B (D50)** — La séquence Phase 3B fait maintenant palette AVANT typo : `chromatic-gamuts → aesthetic-selections → palettes A/B/C → typo (penseurs + gate + designer) → spécimens → penseur visuel`. Audit a confirmé que typo et palette sont indépendantes (subagent palette ne consomme jamais les fonts, planche comparative utilise une font système hardcodée, gates en silos, phases aval consomment les deux post-validation). Renumérotation des Vagues dans SKILL.md de BIG : Vague 2bis → Vague 1 (palettes), Vague 1 → Vague 2 (penseurs typo), Vague 1bis → Vague 2bis (gate fonts), Vague 2 → Vague 2ter (designer typo), Vague 2ter → Vague 3 (spécimens), Vague 2quater → Vague 4 (penseur visuel). Test-big aligné : 3B-2 = palettes, 3B-3 = typo (intervertis).
- **Audit + correction des désalignements test-big ↔ pipeline BIG** — Avant le swap, audit révélant trou critique 3B-2 dans table outputs (typo non listée), erreur d'attribution penseur-c*.md à 3B-5, variantes A/B/C palettes/visual-direction non copiées, scoping-filtered et qualites-v* manquants, pack-* obsolète, Phase 8 packaging absente. Corrigé en intégralité dans `test-big/SKILL.md`. Section "Maintenance test-big (skill miroir)" ajoutée dans `CLAUDE.md` projet pour rappeler à chaque modif BIG de maintenir les 3 tables miroirs (prérequis L106, outputs L216, mapping L336). Renommage 3B-1 (bundle) en 3B-1a (routeur chromatique) + 3B-1b (sélection inspiration) pour refléter les sous-étapes BIG. Ajout Phase 8 (packaging) au mapping.

## 2026-04-23
- **Nouveau skill `/audit-slop` (D47-D49)** — Juge ABSOLU qui complète `audit-elite` (juge RELATIF aux étalons Awards). Audit d'un style-tile contre 4 grilles de règles orthogonales : Craft Moderne (Impeccable + Taste Skill + 6 variantes + GStack), Vercel technique (a11y/perf/typo), BIG pipeline (phase-4 blacklists + scripts Python + principes Phase 4), Perplexity styles datés 2025-2026. Un synthétiseur arbitre les contradictions inter-grilles en fonction du brief/pitch/concept. Architecture validée par test empirique sur concept-1 Camille : 6 grilles indépendantes, écart de 7 points entre la note la plus haute (Perplexity 9/10) et la plus basse (GStack 2/10) — complémentarité confirmée, substitution impossible.
- **Mécanisme de fraîcheur opt-in des sources externes** — Les règles externes sont cachées localement dans `.claude/skills/audit-slop/sources/` (20 fichiers). À chaque `/audit-slop`, le skill propose un check de fraîcheur contre les versions distantes (raw.githubusercontent.com), présente les deltas en rapport écrit, mais n'applique JAMAIS les changements automatiquement — l'utilisateur intègre manuellement dans une session d'optimisation dédiée. Scripts : `lib/freshness-check.sh` et `lib/apply-refresh.sh`. Testé live : 2 deltas GStack détectés dès la première exécution.
- **Format de rapport standardisé** — Chaque agent produit violations binaires + jugements sémantiques + OK + N/A + skippées + score /10. Le synthétiseur calcule un score pondéré (30% Craft + 20% Vercel + 30% BIG + 20% Perplexity), extrait les top-10 violations prioritaires après dédup, arbitre les contradictions avec justification traçable, produit la matrice de couverture et le verdict temporel (DATÉ / CYCLIQUE_RETOUR / ACTUEL / INTEMPOREL / HYBRIDE).

## 2026-04-01
- **Penseur visuel intégré en Phase 3B (D44)** — Nouveau subagent "penseur visuel" (Vague 2quater) entre les spécimens validés et le pitch final. Prescrit la direction visuelle technique complète : type (taxonomie A-G), sujet, cadrage, lumière, composition, aspect ratio, palette hex, matière. Avec ancre stylistique 6 dimensions + gate qualité 6 critères + contrainte diversité sujets. Le pitch final reprend la direction au lieu de la dériver. Testé sur VoltaPilot C2 (stock → correct+ 7.5/10).
- **Skill `/visual-brief` enrichi** — 3 gates (conformité technique, anti-stock, anti-slop IA), double prompt systématique MJ+Recraft, 6ème dimension ancre (registre de réalité), params Recraft externes (palette, dimensions, modèle), demande d'images de niveau en début de session, traduction prescription DA → prompt avec bonnes pratiques anti-dérive MJ.
- **Audit comparatif 88 sites Awards** — Distribution layouts (stacked 27%, full-bleed 23%, split 19%), gap BIG identifié (split 71%, pas de stacked/full-bleed), 8 patterns de layout catalogués, page pédagogique (layering, 3ème couche, surfaces).

## 2026-03-28
- **Extraction Phase 3C en skill standalone `/visual-brief` (D43)** — La génération des prompts visuels MJ/Recraft et l'analyse des images sont extraites de l'orchestrateur BIG et isolées dans un skill séparé, exécuté dans une session Claude Code indépendante. Raison : l'orchestrateur, surchargé de contexte à ce stade du pipeline, ne suivait pas rigoureusement les guides MJ/Recraft (routage ignoré, paramètres incorrects, ancres incomplètes — constaté sur sessions mars 2026). Le skill dédié charge les guides avec un contexte frais.
- Nouveau fichier `ref/visual-direction-guide.md` — Guide DA pour le skill visual-brief : principes de composition visuelle, traduction concept→direction photo/illu, lien entre usage prévu dans le style-tile et type d'image à prompter, anti-patterns visuels, arbre de décision pour recommandation d'intégration CSS.
- Mise à jour SKILL.md Phase 3C — remplacée par une instruction de lancer `/visual-brief` dans une session séparée. L'orchestrateur vérifie la présence de `{brand}-visual-analysis.md` au retour de l'utilisateur.

## 2026-03-15
- **Vérification visuelle typo + palette (Phase 3B-bis)** — Nouvelle étape automatique entre Phase 3B (pitch design) et la présentation utilisateur. Le subagent 3B choisissait ses fonts à l'aveugle (noms textuels dans des pools) ; maintenant, l'orchestrateur génère un specimen HTML (Google Fonts réelles + swatches de palette + texte sur fonds colorés), capture un screenshot Puppeteer, et resume le subagent pour validation visuelle. Si le rendu ne correspond pas à l'intention → correction + re-specimen (max 2 itérations). Raison : les incohérences typo n'étaient découvertes qu'en Phase 4bis (audit DA), soit 2 phases trop tard.
- Nouveau script `lib/font-palette-specimen.mjs` : génère le HTML specimen + capture screenshot via Puppeteer. Même infrastructure que `puppeteer-screenshots.mjs` (Phase 4bis).
- SKILL.md, pipeline-overview.md, ARCHITECTURE.md synchronisés.

## 2026-03-02
- **Dual-tool MJ + Recraft** — Recraft V4 intégré comme deuxième outil de génération visuelle dans Phase 3C. Routage par prompt (pas par concept) : 3 registres Recraft (I1 Flat/Corporate, I2 Line Art, I7 Infographique) pour les illustrations flat/vector/infographies ; 21 registres MidJourney pour tout le reste. Un même concept peut mixer les deux outils (ex: photo hero MJ + illustration flat Recraft).
- Nouveau fichier `ref/recraft-prompting-guide.md` (~200 lignes) : principes V4, template 7 couches, prompting par type BIG, checklist, erreurs courantes. Adapté du framework Recraft officiel (nov 2025 / fév 2026).
- SKILL.md Phase 3C : routage en 2 étapes (R1 choix outil, R2 lecture guide), format brief visuel adapté (champ Outil, gate bifurquée MJ/Recraft, tableau récap avec colonne Outil), message utilisateur mentionnant les deux plateformes.
- Sections Batch 08.4 et 10.5 : mention Recraft pour illustrations flat/vector.
- Phase Logo : nuance ajoutée (Recraft pertinent en 3C mais inadapté pour logos).
- REX logo-generation : ajout mention Recraft V4 pour Phase 3C.
- Guide MJ : zéro modification (non-régression).

## 2026-02-27 (session 2)
- **Territoire Esthétique Client** — nouveau mécanisme de cadrage intégré dans le pipeline BIG. 4ème pilier au même niveau que Tension, Ventre Mou et Curseurs A×B. Activable quand un client fournit des inspirations visuelles (captures, noms de styles, marques admirées). Architecture : Phase 1bis (extraction profil par l'orchestrateur, conditionnel), Phase 2A (diagnostic de compatibilité — ponts naturels / frictions / pistes de réconciliation), Phase 2B (quality gate — résolution obligatoire si incompatible), Phase 3A (injection comme input réel — chaque concept interprète le territoire différemment), Phase 3B (orientation des choix visuels — palette, typo, surface guidés par le territoire), Phase 3C (teinte des prompts MJ). Rétrocompatible à 100% : si pas de préférences, pipeline identique.
- Guides d'interview v1 et v2 : ajout Phase F optionnelle (Point 15 — Inspirations Esthétiques) + section correspondante dans le format Brief Alpha.
- Pipeline-overview.md synchronisé avec les nouvelles étapes.

## 2026-02-27
- **Guide MidJourney unifié** (`ref/midjourney-prompting-guide.md`) : nouveau fichier de référence couvrant 26 registres visuels (6 photos + 7 illustrations + 6 logos + 3 patterns/textures + 4 spécialisés). Chaque registre documente : mode MJ optimal, --stylize, --style, --ar, --no, mots-clés efficaces, pièges spécifiques, prompt exemple, --sref recommandés. Inclut un arbre de décision pour le routage automatique brief → registre, et un mécanisme de check de version MJ. Source unique — les règles MJ dupliquées dans SKILL.md §3C et logo-design-bible.md §17/§19 sont remplacées par des renvois. Le bloc "Anti-références" séparé est supprimé — tous les négatifs intégrés dans le `--no` de chaque prompt.
- Polissage Finition Élite des 4 exemples Batch (2 standard + 2 rupture) : application du principe "Code > Rules" (D20). Shadows multi-couches dans :root (standard uniquement — rupture reste flat/intentionnel), easing physiques nommés (--ease-out-expo, --ease-out-back), tous les `transition: all` remplacés par transitions ciblées (propriétés nommées), tous les `translateY` au hover retirés, hovers enrichis ≥2 propriétés, padding varié entre chapitres. Batch 3 rupture : 4 hovers brutalist ajoutés (n'en avait que 2 pour 1258 lignes). Shadow hardcodée dans Batch 3 standard remplacée par var().
- Séparation Socle de Finition Élite vs Calibrage Créatif : le socle de finition (ombres multi-couches, easing physiques, rythme de spacing, hovers multi-property) est désormais UNIVERSEL et indépendant du curseur A. Le calibrage A ne pilote plus que la COMPOSITION (forme, audace). Raison : "surfaces simples" en A=1 faisait interpréter "pas besoin de sophistication" → CSS basique. Nouveau bloc ~25 lignes dans SKILL.md Phase 4, Gate 11 ajoutée, calibrage A réécrit×3 (Phase 4 + Batch 2 + Batch 3), directive qualité enrichie.
- html-showroom-spec.md : template :root shadows mis à jour (multi-couches obligatoire), transitions avec cubic-bezier nommés (plus de `ease` générique), section "Finition élite" ajoutée en §5.
- Exemples HTML polis (finition élite) : Example A (shadows multi-couches ajoutées, easing physiques, paddings différenciés, hovers enrichis), Example B (shadows multi-layer, easing nommés, translateY retirés des hovers pour conformité Gate 10, padding atmosphere resserré), Example C (glows multi-couches, easing nommés, padding artifact différencié, hovers clip-path enrichis).

## 2026-02-26
- Socle de finition CSS redéfini à 6 techniques obligatoires (oklch, @layer, @property, color-mix, text-wrap, clamp) au lieu de 8. @container et :has() reclassées en "bonus contextuel" (pas systématiquement exigées). Raison : l'audit V8 montre que les ajouter dans les exemples ne change pas l'adoption (0/3), et les forcer via gate produirait du code artificiel — ce sont des patterns d'ingénierie app-level, pas des améliorateurs visuels universels. Gates CSS Moderne mises à jour dans SKILL.md (Phase D4, Phase 4, Batch 2, Batch 3) et html-showroom-spec.md. Exemples HTML conservent les ajouts @container/:has() précédents (usage pertinent).
- Ajout @container et :has() dans les 3 exemples HTML (A: summary-bar + table alert, B: timeline hover, C: config-output). Conservés malgré le reclassement car ce sont des usages légitimes.
- Phase 3B — remplacement des règles de diversité rigides par une directive "Ancrage Scoping" : le Ventre Mou sectoriel devient anti-modèle prescriptif, les signaux visuels de résolution de tension deviennent guide de design, la position ZAG devient compas. Retrait de 5 règles "DOIVENT être DIFFÉRENTS" (harmonies, températures, compositions, artefacts, interactions). Gardé : diversité typo (fonts display/body ≠) et au moins 1 atmosphère non-sombre. Chaque stratégie garde son menu d'options — seule la contrainte de diversité inter-concept est retirée.

## 2026-02-24
- Anti-cliché Phase 3A : directive de profondeur métaphorique forçant l'exploration de 3 niveaux (évident → inattendu) avant de choisir une métaphore. Le premier niveau reste autorisé si justifié, mais pas par défaut.
- Enrichissement exemples domaines 2C : les métaphores trop évidentes (croissance, construction, mouvement, lumière) remplacées par des métaphores de niveau 2-3 (mue, cadastre, balancier, rémanence, etc.)
- Dédup inter-versions : à partir de v2, l'orchestrateur extrait titres + métaphores centrales des versions précédentes et les injecte comme bloc d'exclusion dans le prompt subagent. Compression automatique au-delà de 3 versions.

## 2026-02-23
- Dé-saturation contexte Batch 3 : le subagent ne lit plus le fichier Batch 2 complet (80-100K tokens de SVG paths) mais reçoit un résumé structuré (~500 tokens) extrait par l'orchestrateur avant injection SVG
- Protection style-tile lourd : si le style-tile > 200 Ko (images base64 de Phase 3C), une version allégée est créée automatiquement (data URIs → placeholder) pour les subagents Batch
- Progress logs : les subagents Batch 2 et 3 écrivent un `.progress-batch{n}.log` à chaque étape clé pour la détection de blocages
- Split Batch 3 en 3 subagents séquentiels (ch08, ch09, ch10) : résout le blocage output token limit du subagent monolithique. Squelette HTML créé par l'orchestrateur, assemblage final après les 3 chapitres
- Règles de cohérence inter-chapitres : accents UTF-8, nommage BEM `ch{n}-`, overline color, propriétés physiques, screenshot test
- Audit v11→v12 : correction header .batch-hero, accents manquants, dead code @property, @layer, préfixes classes
- Règle "Show > Tell" dans Batch 3 : chaque carte/bloc décrivant un concept visuel doit l'incarner en CSS (pas de texte descriptif sur fond neutre). Gate de validation ajoutée + renforcement des prompts 08.2, 09.2, 10.3
- Nouvelles variables : `{style_tile_read_path}`, `{batch2_design_summary}`, `{batch3_shared_context}`

## 2026-02-22
- Extension de l'indexation curseur : Phase 3B spécifie maintenant 4 nouveaux champs dans la Direction visuelle — artefact recommandé, philosophie d'interaction, techniques CSS prioritaires, registre atmosphérique. Phase 4 les consomme via 4 directives "CONSULTE le pitch" + Gate 10 (interdit translateY pour A≥2). Règles de diversité ajoutées (artefacts différents, interactions différentes, au moins 1 atmosphère non-sombre).
- Ajout du skill `/test-big` : test runner pour BIG qui permet de reprendre le pipeline à une phase spécifique en réutilisant les artifacts d'une session précédente. Un seul fichier SKILL.md (~286 lignes), pas de ref/ ni examples/ (utilise ceux de BIG).

## 2026-02-20
- CSS moderne 2023-2026 : catalogue de techniques dans html-showroom-spec.md (oklch, @layer, @property, container queries, clip-path, text-wrap, etc.)
- Gate CSS Moderne ajouté aux prompts Phase 4, D4, Batch 2, Batch 3 (minimum 3-4 techniques)
- Diversité de composition voice-block : 9 types de composition, les 3 concepts doivent être différents (imposé en Phase 3B)
- Exemples HTML modernisés : standard (7 techniques) et rupture (8 techniques, dont clip-path)
- Ajout du CLAUDE.md projet (contexte immédiat pour chaque session)
- Mise en place du versioning git + .gitignore
- Déblocage de la diversité créative en Phase 3B + Phase 4 (fonts, palettes, harmonies)

## 2026-02-19
- Ajout des exemples Phase 3 multi-registre (prudent, décalé, rupture)
- Renforcement anti-contamination : INTERDICTIONS ABSOLUES (fonts, palettes, triptyques)

## Avant versioning (≤ 2026-02-13)
- Développement initial du pipeline BIG (10 étapes)
- Migration depuis le système Gemini original (8 fichiers)
- Création des 4 fichiers REX (logo generation, vectorization, lockup, landing demo)
- Création des exemples standard (CANOPY/VoltaPilot) et rupture (FURNACE/Atelier Vermeil)
- Skill LPG (Landing Page Generator) hébergé dans le même projet
